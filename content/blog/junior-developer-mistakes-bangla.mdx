---
title: "জুনিয়র ডেভেলপার হিসেবে যে ভুলগুলো করেছিলাম"
date: "2026-01-29"
description: "২ বছর আগে যদি কেউ এই জিনিসগুলো বলে দিত! নতুন ডেভেলপারদের জন্য সত্যিকারের পরামর্শ।"
tags: ["bangla", "career", "programming", "mistakes"]
published: true
---

২০২২ সালে যখন প্রথম জব জয়েন করি, তখন মনে হয়েছিল আমি সব জানি। React জানি, JavaScript জানি, CSS জানি - আর কী লাগবে?

**Spoiler alert:** অনেক কিছু লাগবে। আর আমি জানতাম না।

আজকে সেই ভুলগুলোর কথা বলব যা আমি করেছিলাম, যাতে তোমরা এড়িয়ে যেতে পারো।

## ভুল #১: Tutorial Hell থেকে বের হতে পারিনি

### আমার সমস্যা

YouTube-এ একের পর এক টিউটোরিয়াল দেখছি:
- "Complete React Course - 12 hours"
- "MERN Stack Full Tutorial"
- "Next.js Crash Course"

দিনের পর দিন দেখছি, follow করছি, নোট নিচ্ছি। কিন্তু যখন নিজে কিছু বানাতে বসি - **blank screen!**

কোথা থেকে শুরু করব বুঝতে পারি না।

### কেন এটা সমস্যা?

টিউটোরিয়ালে সব রেডি করা থাকে:
- Code structure দেওয়া
- Problem ও জানা, solution ও জানা
- Step by step guide

**Real project-এ কিছুই দেওয়া থাকে না।** আপনাকেই সব ঠিক করতে হবে।

### আমি যা করলাম

টিউটোরিয়াল দেখা বন্ধ করে নিজে প্রজেক্ট বানানো শুরু করলাম:

১. **Simple Todo App** - কিন্তু টিউটোরিয়াল দেখে না
২. **Weather App** - API integration শিখলাম
৩. **Blog Site** - Backend, Database সব নিজে করলাম
৪. **E-commerce** - Payment gateway যোগ করলাম

**Result?** ৩ মাসে আমার skill 10x বেড়ে গেল।

### তোমার জন্য পরামর্শ

```
70% সময় - নিজে কোড লেখো
20% সময় - Documentation পড়ো
10% সময় - Tutorial দেখো
```

Tutorial দেখবে শুধু নতুন concept শেখার জন্য। তারপর নিজে implement করো।

## ভুল #২: সব কিছু শিখতে চাইতাম

### আমার ভুল

২০২২ সালে আমার "শিখতে চাই" list:
- ✅ React
- ✅ Angular
- ✅ Vue
- ✅ Svelte
- ✅ React Native
- ✅ Flutter
- ✅ Python
- ✅ Go
- ✅ Rust
- ✅ ... (আরো ২০টা জিনিস)

**Result?** সবই একটু একটু জানি, কিছুই ভালো করে জানি না।

Interview-এ গেলাম:

**Interviewer:** "React-এ কাজ করেছ?"
**আমি:** "হ্যাঁ, কিছু কিছু জানি।"
**Interviewer:** "useCallback আর useMemo-এর difference?"
**আমি:** "আ... উ... মানে..." 😅

### সঠিক approach

**Master one, then expand।**

আমি যা করলাম:
1. **React ভালো করে শিখলাম** (৬ মাস)
2. তারপর **Next.js** (২ মাস)
3. তারপর **TypeScript** (১ মাস)
4. তারপর **Node.js** (৩ মাস)

One by one। Depth-first, not breadth-first।

### Real Example

```jsx
// আগে আমার code (shallow knowledge)
function Component() {
  const [data, setData] = useState([]);
  
  useEffect(() => {
    fetch('/api/data').then(r => r.json()).then(setData);
  }, []);
  
  return <div>{data.map(d => <p>{d}</p>)}</div>;
}

// এখন আমার code (deep knowledge)
function Component() {
  const { data, isLoading, error } = useQuery('data', fetchData);
  
  if (isLoading) return <Spinner />;
  if (error) return <ErrorMessage error={error} />;
  
  return (
    <ErrorBoundary>
      {data?.map(item => (
        <DataCard key={item.id} data={item} />
      ))}
    </ErrorBoundary>
  );
}
```

দেখো, difference টা? এটা শুধু code না, এটা **understanding।**

## ভুল #৩: Documentation পড়তাম না

### সিরিয়াসলি, এটা আমার biggest mistake

যখন কোনো problem হতো:
1. Google করতাম
2. Stack Overflow-এ copy-paste করতাম
3. কাজ হলে খুশি, না হলে আরেকটা solution খুঁজতাম

**কখনো** official documentation পড়তাম না।

### কী হতো?

একদিন senior developer জিজ্ঞেস করল:

**তিনি:** "Redux-এ middleware কীভাবে কাজ করে?"
**আমি:** "Um... মানে... লগিং করে?"
**তিনি:** "Documentation পড়েছো?"
**আমি:** "না..."
**তিনি:** "যাও, পড়ে এসো।"

সেদিন প্রথম React documentation ওপেন করলাম। **Mind blown!** 🤯

এতদিন যে problems face করছিলাম, সব লেখা ছিল ওখানে!

### তোমাদের বলছি

নতুন library/framework শেখার সময়:

**Step 1:** Official documentation পড়ো (skip করো না!)
**Step 2:** Basic example গুলো try করো
**Step 3:** নিজের project-এ use করো
**Step 4:** Stuck হলে documentation-এ search করো

Documentation-এ সাধারণত:
- ✅ Best practices
- ✅ Common mistakes
- ✅ Performance tips
- ✅ Real examples
সব থাকে!

## ভুল #৪: Git제대로 use করতাম না

### আমার Git workflow (আগে)

```bash
git add .
git commit -m "update"
git push
```

**Every. Single. Time.**

Commit message: "update", "fix", "change", "asd;lfkj"

Branch? মানে? Main-এ সরাসরি push করতাম।

### কী problem হলো?

একদিন একটা feature add করলাম। Push করলাম। Production break করে দিল। সব user affected!

Rollback করতে গিয়ে দেখি - আগের কোন commit-এ ফিরতে হবে বুঝতে পারছি না। সব commit-এ লেখা "update" 😭

### এখন যা করি

```bash
# Feature branch তৈরি
git checkout -b feature/user-authentication

# Meaningful commits
git commit -m "feat(auth): add login form validation"
git commit -m "feat(auth): integrate JWT token handling"
git commit -m "test(auth): add login form tests"

# PR তৈরি করি, review নিই
# Merge হলে branch delete করি
```

### তোমাদের জন্য

**Commit message format শেখো:**
```
feat: নতুন feature
fix: bug fix
refactor: code improve
docs: documentation
test: test add করা
```

**Branch strategy follow করো:**
- `main` - production code
- `develop` - development code
- `feature/*` - নতুন features
- `fix/*` - bug fixes

## ভুল #৫: Testing ignore করতাম

### আমার mindset (আগে)

"Testing is for senior developers। আমি junior, আমার দরকার নাই।"

Code লিখতাম, manually check করতাম browser-এ, কাজ করলে done!

### Reality check

একদিন একটা ছোট change করলাম login form-এ।
**Result?** Whole app crash! 💥

কারণ? সেই login form ১৫টা জায়গায় use হচ্ছিল। একটা জায়গায় change করাতে সব ভেঙে গেল।

### এখন

প্রতিটা component-এর জন্য basic test লিখি:

```tsx
// Simple test example
describe('LoginForm', () => {
  it('shows error for invalid email', () => {
    render(<LoginForm />);
    
    const input = screen.getByLabelText('Email');
    fireEvent.change(input, { target: { value: 'invalid' } });
    fireEvent.blur(input);
    
    expect(screen.getByText('Invalid email')).toBeInTheDocument();
  });
});
```

**Benefits:**
- Confident refactoring
- Bugs catch করা সহজ
- Documentation হিসেবে কাজ করে

## ভুল #৬: Senior দের কাছে জিজ্ঞেস করতে ভয় পেতাম

### আমার ভাবনা

"এত simple question জিজ্ঞেস করলে seniors কী ভাববে? Noob ভাববে না তো?"

তাই ঘন্টার পর ঘন্টা একা একা stuck থাকতাম।

### সত্য

একদিন আমার senior নিজে এসে বলল:

**তিনি:** "তুমি এই simple bug-এর জন্য ২ দিন waste করলে কেন? আমাকে বলতে পারতে।"
**আমি:** "আপনি busy ছিলেন..."
**তিনি:** "Listen, তোমার 2 ঘণ্টা stuck থাকার চেয়ে আমার 5 মিনিট দেওয়া better। সবসময় জিজ্ঞেস করো।"

সেদিন শিখলাম - **Senior developers help করতে পছন্দ করে!** তারাও একসময় junior ছিল।

### তোমাদের জন্য

**জিজ্ঞেস করার নিয়ম:**

১. **নিজে চেষ্টা করো** (৩০ মিনিট - ১ ঘন্টা)
২. **Google করো, documentation দেখো**
৩. **তারপরও solve না হলে জিজ্ঞেস করো**

জিজ্ঞেস করার সময়:
```
❌ "এটা কাজ করছে না।"

✅ "আমি X করার চেষ্টা করছি। 
   Y approach try করেছি কিন্তু Z error দিচ্ছে।
   Documentation-এ A section পড়েছি।
   আপনি কি help করতে পারবেন?"
```

## ভুল #৭: Imposter Syndrome ignore করেছিলাম

### আমার অনুভূতি

প্রতিদিন মনে হতো:
- "আমি যথেষ্ট ভালো না"
- "অন্যরা আমার চেয়ে ভালো"
- "কবে জানিনা caught হয়ে যাবো"

রাতে ঘুম আসতো না। Stress-এ ভুগতাম।

### সত্য

সবাই এমন feel করে! সব junior, এমনকি অনেক senior ও।

একদিন আমাদের tech lead বলল:
**"আমার 8 বছর experience আছে, still everyday শিখছি। এটাই normal।"**

### Overcome করার উপায়

১. **Progress track করো**
   - ৩ মাস আগের code দেখো
   - কতটা improve করেছো দেখবে

২. **Small wins celebrate করো**
   - Bug fix করলাম? ✅ Win!
   - New concept শিখলাম? ✅ Win!
   - PR merge হলো? ✅ Win!

৩. **Community-তে যুক্ত হও**
   - Discord servers join করো
   - Twitter-এ developers follow করো
   - বুঝবে সবাই struggle করছে

## শেষ কথা: আমি যদি ২ বছর আগে ফিরে যেতে পারতাম

তাহলে নিজেকে বলতাম:

**"Hey বুদ্ধু,**

Relax করো। তুমি যে ভুলগুলো করছো, সেগুলো normal। প্রতিটা developer এই phase দিয়ে গেছে।

তুমি যথেষ্ট ভালো আছো। শুধু consistent থাকো। প্রতিদিন একটু একটু করে শেখো। তাড়াহুড়ো করো না।

আর হ্যাঁ, **tutorial hell থেকে বের হয়ে এসো। নিজে কোড লেখো!**

তোমার Future Self,
যে এখনও শিখছে। 😊"

---

## তোমাদের জন্য Action Plan

**এই সপ্তাহে করো:**
- [ ] একটা personal project শুরু করো (টিউটোরিয়াল দেখে না)
- [ ] যে technology use করছো তার documentation-এ ১ ঘণ্টা সময় দাও
- [ ] Git commit message improve করো
- [ ] একটা simple test লেখো

**এই মাসে করো:**
- [ ] একটা framework master করো (সব না!)
- [ ] LinkedIn/Twitter-এ active হও
- [ ] Local developer community-তে join করো
- [ ] Blog লেখা শুরু করো (যা শিখছো তা share করো)

**মনে রাখবা:**
> "Everyone you admire was once a beginner. Keep going!"

তোমরাও পারবা। Just don't give up! 💪

আর হ্যাঁ, ভুল করতে ভয় পেয়ো না। ভুল থেকেই সবচেয়ে বেশি শেখা যায়।

Happy coding! 🚀
